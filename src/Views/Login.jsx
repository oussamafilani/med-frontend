import React, { useRef } from 'react';

import Login from '../Components/login/Login'

function Goods() {
    return (
        <div>
                <Login/>

        </div>
      );
}

export default Goods;